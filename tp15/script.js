const config = document.querySelector(".config");
const inicio = document.querySelector(".inicio");
const cerrar = document.querySelector(".nav-ce");
const regis = document.querySelector(".regis");

inicio.addEventListener("click", () => {
  regis
});
